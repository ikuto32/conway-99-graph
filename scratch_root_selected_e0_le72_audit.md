# Selected-root reduction to E0 <= 72

Status: `SELECTED_ROOT_E0_LE_72_VERIFIED`.  
Errors: `0`.

The self-contained side bound selects a root with `S<=69`. Solver-free arguments exclude `E0>=75`; complete local enumerations and independently checked, hash-bound DRUP certificates exclude `E0=74` and `E0=73`. Hence every putative target has a selected root with `E0<=72`.

This does not settle existence: the `E0<=72` region remains.
