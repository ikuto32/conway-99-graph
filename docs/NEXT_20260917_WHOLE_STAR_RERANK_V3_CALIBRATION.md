# Immutable calibration metrics addendum, before any v3 GPU result

This addendum leaves the frozen preparation, four binary copies,128-model population, original objective,5000cold initialization, process budgets and untested112 union16 selection unchanged. The separate calibrated execution wrapper binds this document and its own source into the actual execution inputs, while calling the frozen v3 producer. It computes a separate descriptive metrics report after successful execution; every calibration case remains included regardless of outcome.

Population: the exact16 LP-tested configurations frozen in the v3 preparation manifest. For each of the500 and5000 numerical outputs, report all16 lower/upper endpoints and gaps, plus the mean and median gap across those16 cases. A gap is the difference of the two saved binary-float endpoints. Aggregate mean/median and comparisons are computed as exact rational arithmetic on those binary-float values, with approximate floats only for display. This exact calculation of a numerical statistic does not certify the GPU computation.

Endpoint checks: for every case compare the saved numerical lower to the exact LP interval's lower endpoint and the saved numerical upper to its exact upper endpoint, using exact binary-float-to-rational comparisons. Count lower violations (`numerical lower > exact lower`), upper violations (`numerical upper < exact upper`), their sum among32 endpoint checks, and cases with either violation among16. These checks concern bracketing the saved exact interval; failing one does not on its own show that the numerical value is outside the unknown true optimum. Report every violation by original ID and endpoint.

Ordering: enumerate all120 unordered pairs among the16 calibration cases. An exact order is established only when one case's exact upper endpoint is strictly below the other's exact lower endpoint. All overlapping or touching exact intervals are classified as ambiguous and reported separately, never assigned an exact ordering. For each of the500 and5000 outputs, count pairwise disagreements with the established exact order separately for the numerical upper-score ordering and lower-score ordering. Both orderings are ascending score, then original row ID, exactly matching the selection convention. Report numerical score ties separately while retaining that deterministic tie-break; preserve the individual pair outcomes.

Empirical criteria are separate: mean-gap shrinkage and median-gap shrinkage each require a strict decrease; upper-order improvement and lower-order improvement each require fewer disagreements over the same exact-separated pair population. A combined gap-shrinkage flag requires both gap statistics to decrease. No combined mathematical progress score is formed. A zero-disagreement baseline may remain zero without satisfying a strict-improvement flag. Endpoint counts are diagnostics and do not filter cases or retrospectively alter the shortlist. Ambiguous pairs remain in the recorded population but outside disagreement denominators.

The report labels128 reranked existing models,16 calibration controls,112 next-LP-eligible candidates and120 calibration pairs separately. No percentage of Conway-99 completion follows from these metrics. Numerical intervals and rankings remain heuristic; no exact graph or unrestricted exclusion is claimed. Overall search coverage: UNKNOWN; no validated denominator.
