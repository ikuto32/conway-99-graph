# V3 reranking: exact evaluation of the untested union16

This downstream addendum preserves all frozen v3 ranking/calibration files and selection rules. `acceleration/evaluate_whole_star_rerank_v3.py` is a separately versioned adapter based on the frozen v2 evaluator. It accepts only a genuine `INDEPENDENT_WHOLE_STAR_RERANK_V3_AUDIT_PASS`,128 reranked original models,16 previously LP-tested exclusions and112 eligible cases. The selected16 are the preregistered union8upper+8lower, filling overlap by ascending upper score and breaking ties by original whole-family row ID.

The audit scope is explicit: the earlier128 exact model reconstructions and the new full payload-identity audit establish model reuse; this run does not claim128 new reconstructions or independently complete domains for every ranking case. Three sampled numerical CPU controls validate their own checking scope. The adapter separately binds the prior model audit, payload audit, prepared manifest and the independently reviewed16 prior LP cases. Every selected candidate and original-domain artifact is hash-bound to the new independent ranking report. Whole-family direct native IDs and all alternating-cycle sizes are retained.

Question: do exact original-star LP intervals for these16 previously untested configurations improve the incumbent baseline18481? The objective remains `ORIGINAL_STAR_SIMPLEX_PDHG_V1`, with original84 star-domain simplexes, reciprocity and linear common-neighbor cap violations. No triangle/pair-filtered LP domain is substituted. Numerical ranking/calibration values guide selection only; their values are not certificates.

Each selected configuration receives the unchanged native original-star/pair gate, independent complete-domain checker, exact comparison of all original-domain mask sets to the ranking input,30-second original-star LP, separate rational audit and independent integer certificate replay. The pair audit limit is60seconds. Native star/pair limits and240-second subprocess wall allowance are the frozen v2 chain. Failure, caps or nonpositive bounds remain pending. An independently checked positive lower bound excludes that fixed K only. A numerical or exact zero does not establish a99-vertex graph.

Only the best independently positive strict original-star improvement (`new exact upper < baseline exact lower`) is eligible for the existing30-second precise edge warm start and unchanged strict audit. Failure to obtain that warm state remains pending. All records, logs, certificates and failures are preserved. No SAT/DRAT call or target-wide coverage claim is part of this wave. Overall search coverage: UNKNOWN; no validated denominator.

Producer controls: `acceleration/results/20260917_whole_star_rerank_v3/evaluator_controls.json` records2 positive and31 corrupted handoff fixtures; it does not independently verify mathematics. The evaluator source SHA256 is `6f9d2b3e1eb25dc1dd41640993bc9523ee06722ab45ba730df4affb6c367582f`. The independent result-auditor source at preparation is `acceleration/audit_20260917_rerank_v3_results.py`, SHA256 `e4d70026be4cf1407bb01be39fafca75a13e83fb91f3e32113b3ffada05e40ed`. Its actual PASS report must be supplied before real preflight or execution.

Use the existing locked environment from repository root. The following command uses the genuine result report path supplied by the independent reviewer and a fresh output directory. `--validate-only` launches no solver and creates no execution directory; replacing it with `--execute` runs the authorized evaluation chain after preflight.

```powershell
$env:UV_PROJECT_ENVIRONMENT = 'build/research-venv'
uv run --locked --offline --cache-dir .uv-cache-20260917 python -B acceleration/evaluate_whole_star_rerank_v3.py --ranking acceleration/results/20260917_whole_star_rerank_v3/ranking/summary.json --ranking-audit acceleration/results/20260917_independent_review/rerank_v3_results.json --ranking-auditor acceleration/audit_20260917_rerank_v3_results.py --ranking-auditor-sha256 e4d70026be4cf1407bb01be39fafca75a13e83fb91f3e32113b3ffada05e40ed --baseline-star-audit acceleration/results/20260916_star_guided_round2/recovered_star_shortlist/index_18481/audit.json --out acceleration/results/20260917_whole_star_rerank_v3/shortlist --seconds 30 --audit-seconds 60 --validate-only
```

At preparation, the producer proposes IDs `66083,26550,26032,26520,7820,1403,13933,66377,22772,80772,80275,80477,79845,17877,65244,74337`; these remain pending the independent ranking report and real adapter preflight. This document does not assert an LP process is running. Execution state and final counts belong to saved command/run/audit records.
