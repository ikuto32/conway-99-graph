# Bounded full-adjacency construction attempts at the sharp C

Neither construction attempt produced a graph or an UNSAT certificate.
No conclusion about the existence of a completion, any E0 layer, or the
Conway problem follows from these bounded runs.

Both models allow every reciprocal84-vertex adjacency with zero
same-fibre entries, all prescribed210 block totals of the exact sharp
compression C, and all1,176 root-label equations. Only one edge between
the disjoint supports01 and23 is fixed present. This is safe within this
fixed-C model: that block has a positive prescribed total, and independently
swapping the two exact symbols in its four distinct groups sends any edge
in the block to the chosen representative while preserving C and the
rooted SRG constraints.

The CP-SAT attempt starts with126 within-fibre pair caps and adds further
violated pair caps only after finding an assignment. Its120-second run
returned UNKNOWN throughout; no assignment was found.
Files: `scratch_next_full_c_lazy.py/.json`.

The incremental SAT attempt starts with the block and label equations,
then is designed to append violated pair caps while retaining learnt
clauses. It never found its first assignment in the120-second interval,
so zero pair caps were added. It recorded1,048 bounded solve calls and
approximately1.05 million conflicts; these are runtime observations only.
The artifact reports TIME_LIMIT_UNKNOWN. The native process reported
exit code1 after writing the final JSON; this is explicitly not treated
as successful execution or as mathematical proof. No proof file or
candidate graph was generated by this run.
Files: `scratch_next_full_c_sat.py/.json`.

The exact graph acceptance gate in either script checks all99 degrees
and4,851 unordered pairs. No unverified partial assignment is exported
to submission.txt. These finite attempts are complete and have no live
solver session; the separately recorded two E72 workers continue.

The next useful construction direction is to use the new overlap
weighted-capacity condition to reject incompatible partial placements
before attempting all remaining edges, or to derive additional uniform
conditions. UNKNOWN alone supplies no mathematical reason to expect a
longer run with unchanged settings to succeed; a changed formulation or
new structural information is a better next experiment.
