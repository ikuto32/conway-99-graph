# E0=73, Q>=4 independent audit

Status: **INDEPENDENTLY_VERIFIED** (errors: 0).

The root reduction is self-contained: the separately audited side theorem gives a root with `S(r)<=69`; hence `E0=73` forces `Q>=4`. No external `n3` premise is used.

Independent compression reconstruction found 27 deficit partitions, 79,841,895 labelled placements and 21,699 weighted S7 orbits. The exact Q-capacity test leaves 13 partitions (49,564,179 placements / 13,230 orbits). All 295 stored balanced representatives were independently canonicalized under all 5,040 group permutations; their orbit mass is 865,830. Exact integer overlap minima and exact-Fraction disjoint minima agree row by row.

The independent port traversal tested every one of 4,758,382 complete labelled assignments with Q>=4. Its exact feasible tuple sets agree on all 295 support rows: 980 assignments on 58 supports. Every survivor was also checked by direct matching DFS in all seven groups (6,860 checks).

Boundary: this is a solver-free necessary-branch census, not an exclusion of E0=73 and not a construction of the Conway 99-graph.
