# Optional solver environment recorded in the original Windows/Python 3.12 workspace.
# The validator and its regression checks need only the Python standard library.
python-sat==1.9.dev15
ortools==9.15.6755
numpy==2.5.2
